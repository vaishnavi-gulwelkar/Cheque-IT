import { Component, NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { selectedUserInterface } from '../selectedUserInterface';

export const selectedUser: selectedUserInterface[] = 
[
];
